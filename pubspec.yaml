name: hyper_lpr
description: HyperLPR Flutter plugin.
version: 0.0.1+1
author:
homepage: http://maxlee.xyz

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: xyz.max.hyper_lpr
        pluginClass: HyperLprPlugin
      ios:
        pluginClass: HyperLprPlugin
